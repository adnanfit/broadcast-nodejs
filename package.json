{
  "name": "sock",
  "version": "1.0.0",
  "main": "api/socket.js",
  "license": "MIT",
  "dependencies": {
    "@vercel/node": "^3.2.27",
    "cors": "^2.8.5",
    "express": "^4.21.1",
    "socket.io": "^4.8.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon api/socket.js",
    "start": "nodemon api/socket.js",
    "deploy": "sudo chmod 644 .git/index && git pull origin main && npm install && sudo pm2 restart 1"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
